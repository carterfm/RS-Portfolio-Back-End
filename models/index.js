// Links to other model files and then exporting will go here
// Should I just go ahead and make this scalable? I guess I don't see a good reason not to, right? I could always adapt this to be a more 
const Artwork = require('./Artwork.js');
const Update = require('./Update.js');
const User = require('./User.js');

module.exports = { Artwork, Update, User };